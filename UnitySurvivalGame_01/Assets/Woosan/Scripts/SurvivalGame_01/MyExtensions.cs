using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//확장 메서드 구현부
namespace WoosanStudio.RhythmGame
{
    //Detector에서 사용하는 확장 메서드 Cam컨트롤러와 통신을 위해
    /*public static class DetectorEx
    {
        //트리거에 Enter상황에 호출
        public static void EnterOnDetector(this Detector detector,DetectorType type)
        {
            Debug.Log("진입 가속도 = " + TouchController.instance.GetAcceleration());
            //CamController.instance.EnterOnDetector(type,TouchController.instance.GetAcceleration());
        }

        //트리거에 Exit상황에 호출
        public static void ExitOnDetector(this Detector detector,DetectorType type)
        {
            Debug.Log("탈출 가속도 = " + TouchController.instance.GetAcceleration());
            //CamController.instance.ExitOnDetector(type,TouchController.instance.GetAcceleration());
        }
    }*/
}
